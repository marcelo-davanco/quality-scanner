{
  "name": "scanner",
  "root": "apps/scanner",
  "sourceRoot": "apps/scanner",
  "projectType": "application",
  "targets": {
    "build": {
      "command": "docker build -t quality-scanner:latest .",
      "options": {
        "cwd": "apps/scanner"
      }
    }
  }
}
