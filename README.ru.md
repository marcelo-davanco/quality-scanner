# Quality Scanner

> 🌐 **Переводы:** [English](./README.md) · [Português](./README.pt-BR.md) · [中文](./README.zh-CN.md) · [Español](./README.es.md) · [हिन्दी / اردو](./README.hi.md)

Конвейер контроля качества кода на базе Docker для проектов NestJS/TypeScript, работающий на SonarQube Community Edition. Выполняет 10 автоматизированных шагов анализа — от обнаружения секретов до безопасности инфраструктуры — и генерирует JSON-отчёт для каждого сканирования.

## Требования

- **Docker** и **Docker Compose**
- **Node.js** >= 18
- **npm** или **yarn**

> ⚠️ На macOS/Linux увеличьте лимит виртуальной памяти, необходимый SonarQube:
> ```bash
> sudo sysctl -w vm.max_map_count=524288
> ```

## Быстрый старт

### 1. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните значения. Единственное обязательное изменение для первого запуска — `SONAR_TOKEN` (см. шаг 2).

### 2. Запуск SonarQube

```bash
docker compose up -d
```

Подождите ~1 минуту и откройте **http://localhost:9000**.

- **Логин по умолчанию:** `admin` / `admin`
- При первом входе система предложит сменить пароль.

### 3. Генерация токена доступа

1. Перейдите в **My Account** → **Security** → **Generate Tokens**
2. Создайте токен типа **Project Analysis Token**
3. Скопируйте токен и укажите его в `.env`:

```env
SONAR_TOKEN=your_token_here
```

### 4. Запуск сканера

```bash
# Сканировать текущую директорию
./scan.sh .

# Сканировать любой Node.js/NestJS проект
./scan.sh /path/to/your/project
```

Контейнер сканера:

1. Установит зависимости проекта
2. Выполнит все 10 шагов анализа
3. Сохранит JSON-отчёты в `./reports/<date>/<scan-id>/`

### 5. Просмотр результатов

- **Дашборд SonarQube:** http://localhost:9000/dashboard?id=your-project
- **Локальные отчёты:** `./reports/`

---

## Шаги анализа

| Шаг | Инструмент | Что проверяет |
|-----|------------|---------------|
| 1 | **Gitleaks** | Захардкоженные секреты и учётные данные |
| 2 | **TypeScript** | Ошибки компиляции |
| 3 | **ESLint** | Правила качества кода (централизованная конфигурация) |
| 4 | **Prettier** | Форматирование кода (централизованная конфигурация) |
| 5 | **npm audit** | Уязвимости зависимостей |
| 6 | **Knip** | Мёртвый код (неиспользуемые экспорты, файлы, зависимости) |
| 7 | **Jest** | Тесты + покрытие |
| 8 | **SonarQube** | Статический анализ + quality gate |
| 9 | **Spectral** | Валидация контракта OpenAPI *(опционально)* |
| 10 | **Trivy** | Безопасность инфраструктуры (IaC) *(опционально)* |

---

## Локальный quality gate перед push

Запустите те же проверки локально перед отправкой кода:

```bash
chmod +x quality-gate.sh
./quality-gate.sh
```

---

## API Lint — Валидация контракта OpenAPI (Шаг 9)

Валидирует контракты OpenAPI/Swagger с помощью **Spectral**.

### Активация

```bash
# Через переменную окружения
ENABLE_API_LINT=true ./scan.sh /path/to/project

# Через docker-compose
ENABLE_API_LINT=true docker compose --profile scan up scanner
```

### Что валидируется

- Все маршруты имеют маппинг ответа `400`
- Пути используют `kebab-case` (например, `/my-resource`)
- Свойства схем используют `camelCase`
- Каждая операция содержит `operationId`, `description`, `summary` и `tags`
- Пути не заканчиваются на `/`
- Ответы `200`/`201` содержат `content`

### Конфигурация

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `ENABLE_API_LINT` | `false` | Включить/отключить этот шаг |
| `API_LINT_SEVERITY` | `warn` | `warn` = только отчёт, `error` = блокировать конвейер |
| `OPENAPI_FILE_PATH` | *(авто-определение)* | Ручной путь к файлу OpenAPI |

Файл OpenAPI определяется автоматически (`swagger.json`, `openapi.yaml` и т.д.). Для настройки правил отредактируйте `scanner/configs/.spectral.yml`. Полное руководство: [`scanner/configs/README.md`](./scanner/configs/README.md).

---

## Infra Scan — Безопасность инфраструктуры (Шаг 10)

Сканирует `Dockerfile`, `docker-compose.yml` и манифесты Kubernetes с помощью **Trivy**.

### Активация

```bash
# Через переменную окружения
ENABLE_INFRA_SCAN=true ./scan.sh /path/to/project

# Через docker-compose
ENABLE_INFRA_SCAN=true docker compose --profile scan up scanner
```

### Что сканируется

| Тип | Обнаруживаемые файлы | Типичные проблемы |
|-----|----------------------|-------------------|
| **Dockerfile** | `Dockerfile`, `Dockerfile.*` | Тег `latest`, отсутствие `USER`, отсутствие `HEALTHCHECK`, использование `ADD` |
| **docker-compose** | `docker-compose.yml`, `compose.yaml` | `privileged: true`, открытые порты, опасные тома |
| **Kubernetes** | `deployment.yaml`, `service.yaml` и др. | `hostNetwork`, отсутствие `securityContext`, нет лимитов ресурсов |

### Конфигурация

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `ENABLE_INFRA_SCAN` | `false` | Включить/отключить этот шаг |
| `INFRA_SCAN_SEVERITY` | `HIGH` | Минимальный уровень блокировки: `CRITICAL`, `HIGH`, `MEDIUM`, `LOW` |
| `SCAN_DOCKERFILE` | `true` | Включить сканирование Dockerfile |
| `SCAN_K8S` | `true` | Включить сканирование манифестов Kubernetes |
| `SCAN_COMPOSE` | `true` | Включить сканирование docker-compose |

Для настройки политик безопасности отредактируйте `scanner/configs/trivy-policy.yaml`. Полное руководство: [`scanner/configs/README.md`](./scanner/configs/README.md).

---

## Полезные команды

| Команда | Описание |
|---------|----------|
| `docker compose up -d` | Запустить SonarQube |
| `docker compose down` | Остановить SonarQube |
| `docker compose down -v` | Остановить и удалить все данные |
| `docker compose logs -f sonarqube` | Просмотр логов SonarQube |
| `./scan.sh /path/to/project` | Запустить полный анализ |
| `./quality-gate.sh` | Запустить локальные проверки перед push |

---

## Структура проекта

```text
quality-scanner/
├── docker-compose.yml          # SonarQube + PostgreSQL + Scanner
├── sonar-project.properties    # Конфигурация сканера
├── quality-gate.sh             # Локальный quality gate перед push
├── run-sonar.sh                # Автономный скрипт анализа SonarQube
├── scan.sh                     # Docker-обёртка для сканера
├── .env.example                # Шаблон переменных окружения
├── scanner/
│   ├── Dockerfile              # Образ сканера
│   ├── entrypoint.sh           # 10-шаговый конвейер (контейнер)
│   ├── configs/
│   │   ├── .eslintrc.js        # Централизованные правила ESLint
│   │   ├── .prettierrc         # Конфигурация форматирования Prettier
│   │   ├── .gitleaks.toml      # Правила обнаружения секретов
│   │   ├── .spectral.yml       # Правила OpenAPI/Swagger
│   │   ├── trivy-policy.yaml   # Политики безопасности Trivy
│   │   └── README.md           # Руководство по конфигурации
│   ├── scripts/
│   │   ├── swagger-lint.sh     # Скрипт линтинга OpenAPI
│   │   └── infra-scan.sh       # Скрипт безопасности инфраструктуры
│   └── test/
│       ├── fixtures/           # Безопасные/небезопасные тестовые фикстуры
│       ├── test-api-lint.sh    # Тесты API Lint
│       └── test-infra-scan.sh  # Тесты Infra Scan
├── quality-configs/            # Конфигурации локального quality gate
├── dashboard/                  # Дашборд результатов (Next.js)
├── example-nestjs/             # Пример проекта NestJS
├── .gitignore
├── LICENSE
└── README.md
```

---

## Устранение неполадок

### SonarQube не запускается

```bash
# Проверить логи
docker compose logs sonarqube

# Типичное решение на Linux/macOS
sudo sysctl -w vm.max_map_count=524288
```

### Ошибка нехватки памяти

Добавьте в сервис `sonarqube` в `docker-compose.yml`:

```yaml
deploy:
  resources:
    limits:
      memory: 2g
```

### Сканер не находит файлы

Убедитесь, что `sonar-project.properties` находится в корне проекта и все пути указаны верно.

---

## Вклад в проект

Мы рады вашему участию! Прочитайте [CONTRIBUTING.md](./CONTRIBUTING.md) перед отправкой pull request.

## Лицензия

[MIT](./LICENSE)
